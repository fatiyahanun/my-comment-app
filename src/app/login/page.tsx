// Make sure the file exists at ../components/LoginForm.tsx or ../components/LoginForm/index.tsx
import LoginForm from '../components/LoginForm';

export default function LoginPage() {
  return <LoginForm />;
}